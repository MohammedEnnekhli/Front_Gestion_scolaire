import { RunDummyFuncDirective } from './run-dummy-func.directive';

describe('RunDummyFuncDirective', () => {
  it('should create an instance', () => {
    const directive = new RunDummyFuncDirective();
    expect(directive).toBeTruthy();
  });
});
